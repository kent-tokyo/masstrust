# Pinned for reproducibility. massspecgym pulls in its own compatible
# torch/rdkit/torch_geometric/pytorch-lightning transitively (see its own
# pyproject.toml) — do not re-pin those here, it invites conflicts. Re-freeze
# this file (`pip freeze > requirements.lock.txt`) after a successful install
# if you need the full resolved transitive set for exact reproduction.
massspecgym==1.3.1
huggingface_hub==0.26.2
