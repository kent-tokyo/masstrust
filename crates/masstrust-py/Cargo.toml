[package]
name = "masstrust-py"
version = "0.1.0"
edition = "2021"

[lib]
name = "masstrust"
crate-type = ["cdylib", "rlib"]

[features]
extension-module = ["pyo3/extension-module"]

[dependencies]
masstrust-core = { path = "../masstrust-core" }
pyo3 = "0.22"
serde_json = "1"
