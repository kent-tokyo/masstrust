# Pinned for reproducibility. massspecgym pulls in its own compatible
# torch/rdkit/torch_geometric/pytorch-lightning/huggingface_hub transitively
# (see its own pyproject.toml) — do not re-pin those here, it invites
# conflicts (confirmed: massspecgym==1.3.1 hard-requires
# huggingface-hub==0.23.2 exactly; a separate top-level pin here previously
# conflicted with it and made this file uninstallable as written).
# `run_baseline.py` writes the full resolved transitive set to
# requirements.lock.txt (and its hash to manifest.json) automatically at the
# end of every real run — no manual re-freeze needed.
massspecgym==1.3.1

# pytorch-lightning==2.2.5 (pulled in by massspecgym above) imports
# lightning_fabric, which still uses the legacy
# pkg_resources.declare_namespace() pattern — removed from setuptools>=81
# (confirmed: a plain `pip install -r requirements.txt` in a fresh venv without
# this pin fails with `ModuleNotFoundError: No module named 'pkg_resources'` on
# `import pytorch_lightning`). Not massspecgym's bug to fix; pin around it here.
setuptools<81
